{
  "name": "dsh-auth-docker",
  "private": true,
  "description": "Docker workspace: Better Auth and extra DeepSeek Harness bundles",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test"
  },
  "engines": {
    "node": "^22.19 || >=24"
  },
  "license": "MIT",
  "packageManager": "pnpm@10.14.0",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/xuhongjia/dsh-auth-docker.git"
  }
}
